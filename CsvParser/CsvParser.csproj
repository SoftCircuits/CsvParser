<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <PackageId>SoftCircuits.CsvParser</PackageId>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Authors>Jonathan Wood</Authors>
    <Company>SoftCircuits</Company>
    <Description>CsvParser makes it easy to work with comma-separated-values (CSV) files. It supports column values that contain commas, quotes or other special characters. It even supports column values that span multiple lines. In addition, you can optionally change settings such as the column delimiter character, the quote character or how blank lines are handled.

Use the CsvWriter class to write a CSV file. Use the CsvReader class to read a CSV file.</Description>
    <Copyright>Copyright © 2019 SoftCircuits</Copyright>
    <PackageLicenseFile>License.txt</PackageLicenseFile>
    <PackageProjectUrl>https://github.com/SoftCircuits/CsvParser</PackageProjectUrl>
    <RepositoryUrl>https://github.com/SoftCircuits/CsvParser</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>csv tsv csv-parser parser</PackageTags>
    <PackageReleaseNotes>CsvReader constructors now allow you to specify the character encoding and/or the byte order detection option; CsvWriter constructors now allow you to specify the character encoding; Optimized CsvReader.ReadRow() method to minimize memory allocations; Added additional methods exposing underlying stream functionality; BREAKING CHANGE: CsvReader.ReadRow() argument now passed by reference</PackageReleaseNotes>
    <NeutralLanguage>en-US</NeutralLanguage>
    <Version>1.1.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\License.txt">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
  </ItemGroup>

</Project>
